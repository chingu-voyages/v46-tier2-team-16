import styles from './RecepieCard.module.css';

const Card = () => {
    return (
        <div style={styles.container}>
            <h1 style={styles.title}>Card</h1>
        </div>
    );
};

export default Card;
